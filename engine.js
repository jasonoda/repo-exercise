var firstName = "Alice";
var lastName = "Fields"

function getName(first, last){

    var name = first +" "+ last;
    console.log(name);

}

getName(firstName, lastName);

// added for second commit
getName(firstName, lastName);

// this is my third commit