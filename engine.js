var firstName = "Jay";
var lastName = "Smith"

function getName(first, last){

    var name = first +" "+ last;
    console.log(name);

}

getName(firstName, lastName);

// added for second commit
getName(firstName, lastName);

// this is my forth commit